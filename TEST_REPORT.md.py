# Отчёт о результатах автоматизированного тестирования платформы 21vek.by
Дата прогона: 1 августа 2026 г.
Инженер автоматизации: Валерий (QA Automation Engineer)
Стек: Python 3.12, Playwright, Pytest, SQLite, Allure, Docker

---

## 1. Резюме покрытия (Test Coverage Summary)
В рамках дипломного проекта спроектирована и внедрена комплексная тестовая инфраструктура. Фреймворк успешно покрывает 5 изолированных бизнес-модулей маркетплейса, страхуя критические цепочки и минимизируя риски ухода пользователей.

*   Всего спроектировано тест-кейсов: 46
*   Успешно пройдено (Passed): 41 (89.1%)
*   Упало по таймауту защиты продакшена (Failed): 5 (10.9%)
*   Стабильность поискового модуля: 100% (25 из 25 тестов в глухой зелёной зоне)

---

## 2. Архитектурный аудит и лучшие практики (Best Practices)
Фреймворк полностью соответствует промышленным стандартам разработки (Middle QA уровня):
1.  Паттерн POM (Page Object Model): Логика страниц (локаторы, методы ввода) полностью изолирована от самих тестовых сценариев и проверок (assert), что гарантирует дешевую поддержку кода.
2.  Каноническое ООП: Конструкторы базовых и дочерних страниц переведены на стандартный метод __init__ с передачей контекста через super().__init__(page).
3.  Data-Driven Testing (DDT): Матрица позитивного поиска (22 товара) вынесена из кода тестов в локальную базу данных **SQLite (test_data.db)**. Тесты динамически извлекают параметры через SQL-запросы SELECT, изолируя тестовые данные.
Интеграционное тестирование:е:** Реализован изолированный сетевой тест API каталога смартфона с валидацией структуры JSON и статус-кода 200 OK.
Эмуляция Mobile UI:I:** Настроен модуль тестирования адаптивной верстки под разрешение экрана iPhone 14 для верификации мобильного бургер-меню.

---

## 3. Анализ дефектов и инфраструктурные решения
В ходе генерального прогона зафиксировано 5 падений (TimeoutError) исключительно в модуле живой корзины (test_cart.py).

Причина:а:** Живой продакшен маркетплейса 21vek.by использует агрессивные алгоритмы анти-фрод и анти-бот защиты. При высокой частоте симуляции кликов (события dispatch_event робота) в рамках одной сессии, бэкенд временно замораживает элементы страницы для программных сессий.
Решение инженера:а:**
    *   Таймауты ожидания селекторов в корзине превентивно увеличены до 25 секунд.
    *   В конфигурационный файл pytest.ini интегрирован промышленный плагин борьбы с flaky-тестами: --reruns 1 --reruns-delay 2. При нестабильности шлюза система даёт серверу отвиснуть 2 секунды и повторяет шаг, что минимизирует ложные падения в CI/CD конвейере.

---

## 4. Заключение (ROI автоматизации)
Созданный фреймворк полностью автономен, очищен от системного кэша компиляции (__pycache__), снабжен жестким файлом ограничений .gitignore и готов к интеграции в любой CI/CD пайплайн. Проект полностью закрывает критерии готовности (Definition of Done) и готов к сдаче.